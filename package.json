{
  "name": "fix-common-accessibility-mistakes",
  "version": "1.0.0",
  "private": true,
  "description": "This is a project to show common mistakes and improvements for forms and other interactions designed using GOV.UK Frontend.",
  "keywords": [
    "govuk",
    "accessibility"
  ],
  "homepage": "https://fix-common-accessibility-mistakes.x-govuk.org",
  "bugs": {
    "url": "https://github.com/x-govuk/fix-common-accessibility-mistakes/issues"
  },
  "license": "MIT",
  "type": "module",
  "main": "eleventy.config.js",
  "scripts": {
    "prebuild": "rimraf _site",
    "build": "eleventy",
    "lint": "npm run lint:prettier && npm run lint:js && npm run lint:scss",
    "lint:fix": "npm run lint:prettier:fix && npm run lint:js:fix && npm run lint:scss:fix",
    "lint:js": "eslint '**/*.js'",
    "lint:js:fix": "eslint '**/*.js' --fix",
    "lint:prettier": "prettier . --check",
    "lint:prettier:fix": "prettier . --write",
    "lint:scss": "stylelint '**/*.scss'",
    "lint:scss:fix": "stylelint '**/*.scss' --fix",
    "start": "eleventy --serve --quiet"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.5",
    "@x-govuk/govuk-eleventy-plugin": "^9.0.0",
    "gray-matter": "^4.0.3",
    "iframe-resizer": "^4.4.5",
    "js-beautify": "^1.15.4",
    "rimraf": "^6.1.3"
  },
  "devDependencies": {
    "@x-govuk/eslint-config": "^0.0.2",
    "prettier": "^3.8.3",
    "stylelint": "^17.12.0",
    "stylelint-config-gds": "^3.0.0",
    "stylelint-order": "^8.1.1"
  },
  "engines": {
    "node": ">=22"
  }
}
